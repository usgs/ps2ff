NP = integer(min=1, default=1)
datadir = string(default=data)
rup_dim_model = magScalingType(default=WC94)
mech = mechType(default=A)
LW = boolean(default=False)
AR = float(default=1.0)
min_seis_depth = float(min=0, default=0)
max_seis_depth = float(min=0, default=20)
mindip_deg = float(min=0, default=0.0)
maxdip_deg = float(max=90, default=90.0)
ndip = integer(min=1, default=19)
ntheta = integer(min=1, default=73)
nxny = integer(min=1, default=50)
neps = integer(min=1, default=10)
trunc = float(min=0, default=3)
minmag = float(default=4.0)
maxmag = float(default=9.0)
dmag = float(min=0, default=0.25)
minepi = float(min=0, default=0.1)
maxepi = float(min=0, default=1000.0)
nepi = integer(min=1, default=41)
nz = integer(default=1)
zhyp = float(default=0.0)
bytheta = boolean(default=False)
M = float(default=6.0)
